# Memories

This directory stores persistent learnings and context that agents should remember across sessions.

## Format

Each memory file should be a markdown file named descriptively, e.g. `nvidia-quirks.md`, `package-gotchas.md`.

## Current Memories

- `project-context.md` — Key facts about this dotfiles project and the system it configures.
